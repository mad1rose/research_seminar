# Runtime dependencies for the web app + Gunicorn (no pytest).
# Used by the Docker `prod` image target. Keep in sync with requirements.txt
# except omit test-only packages.

Pillow>=10.0.0
numpy==2.2.1
scipy==1.15.1
matplotlib==3.10.0
music21==9.3.0
nbformat==5.10.4
flask==3.1.0
gunicorn>=23.0.0
