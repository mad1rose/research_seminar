# Version control
.git
.gitignore

# Python / tooling
.venv
venv
env
__pycache__
*.py[cod]
*$py.class
.pytest_cache
.mypy_cache
.ruff_cache
.hypothesis

# Large / local-only (mount at runtime; see README)
data

# Optional inputs not needed for web image
songs

# Docs and tests (not required to run the web app)
docs
tests
*.ipynb

# IDE / OS
.idea
.vscode
*.swp
.DS_Store
Thumbs.db

# Env files with secrets (use .env.example as template; do not use `.env.*` — it would exclude `.env.example`)
.env
.env.local
